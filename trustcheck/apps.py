from django.apps import AppConfig


class TrustcheckConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'trustcheck'
    verbose_name = 'TrustCheck — IT 외주 계약 검증·분쟁 분석'
