# /etc/nginx/conf.d/websocket.conf
#
# http 컨텍스트에 있어야 한다. WebSocket 업그레이드 요청에만 Connection: upgrade 를
# 붙이고, 일반 요청에는 close 를 준다.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
