# 프리미엄 구독 시스템 - 앱 개발 가이드

## 📋 개요

게임 하니 앱은 3가지 사용자 유형을 지원합니다:
1. **무료 사용자**: 구독 불가 (로그인만 가능)
2. **광고 프리미엄권 사용자** (7일): 1개 게임만 구독 가능
3. **구독 프리미엄권 사용자** (30일): 무제한 구독 가능

---

## 🔄 역할 분담

### ✅ 백엔드 (이미 구현 완료)

1. **프리미엄 상태 관리**
   - `PremiumSubscription` 모델
   - 구독 유형: `free_ad` (광고 7일), `premium` (유료 180일)
   - 만료일 관리

2. **구독 제한 검증**
   - 무료 사용자: 구독 요청 시 403 에러 반환
   - 광고권: 1개 게임 초과 시 403 에러 반환
   - 구독권: 무제한 허용

3. **푸시 알림 필터링**
   - 활성 프리미엄 사용자에게만 푸시 발송
   - 만료된 구독은 자동 제외

4. **API 엔드포인트**
   - `GET /api/premium/status/` - 프리미엄 상태 조회

---

### 📱 앱에서 구현해야 할 것

#### 1. 프리미엄 상태 UI 표시

**API 호출:**
```typescript
GET /api/premium/status/
Authorization: Bearer {access_token}

Response:
{
  "is_premium": true,
  "expires_at": "2025-12-19T00:00:00Z",
  "subscription_type": "free_ad",  // 'free_ad' 또는 'premium'
  "max_games": 1,  // free_ad: 1, premium: null (무제한)
  "subscribed_games_count": 2,
  "can_subscribe_more": false
}
```

**UI 예시:**
```
┌─────────────────────────────┐
│ 내 구독 정보                │
│                             │
│ 📺 광고 프리미엄권 (7일)    │
│ 만료: 2025-12-19            │
│                             │
│ 구독 가능: 1개 중 1개 사용  │
│                             │
│ [프리미엄 구독하기]         │
└─────────────────────────────┘
```

#### 2. 광고 시청 기능 구현

**광고 시청 성공 후 백엔드 호출 (예상 API - 구현 필요 시 요청):**
```typescript
POST /api/premium/activate-ad/
Authorization: Bearer {access_token}

Response:
{
  "success": true,
  "subscription_type": "free_ad",
  "expires_at": "2025-12-26T00:00:00Z"  // 현재 시각 + 7일
}
```

**플로우:**
1. 사용자가 "광고 보고 무료로 시작하기" 버튼 클릭
2. AdMob/AppLovin 등 광고 SDK 호출
3. 광고 시청 완료 시 백엔드 API 호출
4. 백엔드에서 `PremiumSubscription` 생성 (`subscription_type='free_ad'`, `expires_at=now+7일`)
5. UI 업데이트

#### 3. 프리미엄 구독 결제 (토스 페이/인앱결제)

**결제 성공 후 백엔드 호출 (예상 API - 구현 필요 시 요청):**
```typescript
POST /api/premium/activate-subscription/
Authorization: Bearer {access_token}
Content-Type: application/json

{
  "order_id": "ORDER_2025_XXX",  // 인앱결제 주문 ID
  "receipt": "..."  // iOS receipt 또는 Android purchase token
}

Response:
{
  "success": true,
  "subscription_type": "premium",
  "expires_at": "2026-01-19T00:00:00Z"  // 현재 시각 + 30일
}
```

**플로우:**
1. 사용자가 "프리미엄 구독하기" 버튼 클릭
2. 토스 페이 또는 인앱결제 SDK 호출
3. 결제 성공 시 백엔드 API 호출
4. 백엔드에서 `PremiumSubscription` 생성/업데이트
5. UI 업데이트

#### 4. 구독 버튼 상태 관리

**무료 사용자:**
```
┌─────────────────────────────┐
│ 메이플스토리                │
│ 공지사항                    │
│                             │
│ [🔒 구독하려면 광고 시청]   │
│ [💎 프리미엄 구독하기]      │
└─────────────────────────────┘
```

**광고권 사용자 (1/1 사용 중):**
```
┌─────────────────────────────┐
│ 로스트아크                  │
│ 이벤트                      │
│                             │
│ [🚫 광고권은 1개만 구독 가능]│
│ [💎 프리미엄으로 업그레이드] │
└─────────────────────────────┘
```

**광고권 사용자 (0/1 사용 중):**
```
┌─────────────────────────────┐
│ 메이플스토리                │
│ 공지사항                    │
│                             │
│ [🔔 구독하기]              │
└─────────────────────────────┘
```

**프리미엄 사용자:**
```
┌─────────────────────────────┐
│ 메이플스토리                │
│ 공지사항                    │
│                             │
│ [🔔 구독하기]              │
└─────────────────────────────┘
```

#### 5. 구독 요청 시 에러 처리

```typescript
async function subscribeToGame(gameId: string, category: string) {
  try {
    const response = await fetch('/api/subscriptions/', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${accessToken}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({ game: gameId, category })
    });

    if (response.status === 403) {
      const error = await response.json();

      if (error.error.includes('광고를 시청하거나')) {
        // 무료 사용자
        showAdOrPremiumPrompt();
      } else if (error.error.includes('1개 게임만')) {
        // 광고권 제한 초과
        showPremiumUpgradePrompt();
      }
    }
  } catch (error) {
    console.error(error);
  }
}
```

#### 6. 프리미엄 만료 알림

**앱 시작 시 체크:**
```typescript
async function checkPremiumStatus() {
  const status = await fetch('/api/premium/status/');
  const data = await status.json();

  if (data.is_premium) {
    const daysLeft = getDaysUntilExpiration(data.expires_at);

    if (daysLeft <= 3) {
      showNotification(`프리미엄이 ${daysLeft}일 후 만료됩니다.`);
    }
  }
}
```

---

## 📊 전체 플로우 차트

```
사용자 앱 진입
    ↓
토스 로그인
    ↓
프리미엄 상태 조회 (/api/premium/status/)
    ↓
┌───────────────────────────────┐
│ 프리미엄 없음 (무료 사용자)   │
│   ↓                           │
│ "광고 보기" 또는 "프리미엄 구독"│
│   선택 UI 표시                │
└───────────────────────────────┘
    ↓
┌───────────────────────────────┐
│ 광고 시청                     │
│   ↓                           │
│ POST /api/premium/activate-ad/│
│   ↓                           │
│ free_ad 프리미엄 활성화 (7일) │
└───────────────────────────────┘
    또는
┌───────────────────────────────┐
│ 프리미엄 결제                 │
│   ↓                           │
│ 토스 페이/인앱결제            │
│   ↓                           │
│ POST /api/premium/activate-subscription/│
│   ↓                           │
│ premium 프리미엄 활성화 (180일)│
└───────────────────────────────┘
    ↓
게임 소식 구독
    ↓
POST /api/subscriptions/
    ↓
┌───────────────────────────────┐
│ 백엔드 검증                   │
│ - 무료 사용자: 403            │
│ - free_ad: 1개 초과 시 403    │
│ - premium: 무제한 OK          │
└───────────────────────────────┘
    ↓
구독 성공
    ↓
새 게임 소식 발생 (크롤링)
    ↓
백엔드에서 활성 프리미엄 사용자만 필터링
    ↓
토스 푸시 알림 발송
```

---

## 🚨 주의사항

### 1. 구독 제한 검증은 백엔드에서 처리
- 앱은 UI만 제어하면 됨
- 실제 검증은 백엔드 API에서 수행

### 2. 프리미엄 만료 체크
- 백엔드에서 자동으로 만료된 구독 삭제
- 앱은 `/api/premium/status/`로 주기적 확인

### 3. 푸시 알림
- 활성 프리미엄 사용자에게만 발송됨 (백엔드 처리)
- 앱은 별도 처리 불필요

---

## 🔗 API 엔드포인트 요약

### 기존 API (이미 구현됨)
- `GET /api/premium/status/` - 프리미엄 상태 조회
- `POST /api/subscriptions/` - 게임 소식 구독
- `DELETE /api/subscriptions/{id}/` - 구독 취소
- `GET /api/subscriptions/` - 내 구독 목록

### 필요 시 추가 요청할 API
- `POST /api/premium/activate-ad/` - 광고 시청 후 프리미엄 활성화
- `POST /api/premium/activate-subscription/` - 결제 후 프리미엄 활성화

---

## 💡 UI/UX 권장사항

### 1. 프리미엄 가치 강조
```
┌─────────────────────────────┐
│ 🎮 프리미엄으로 업그레이드   │
│                             │
│ ✅ 모든 게임 무제한 구독     │
│ ✅ 30일간 광고 없이         │
│ ✅ 실시간 푸시 알림         │
│                             │
│ 월 4,900원                  │
│ [지금 시작하기]             │
└─────────────────────────────┘
```

### 2. 광고권 제한 안내
```
┌─────────────────────────────┐
│ 광고 프리미엄권 (무료 7일)   │
│                             │
│ • 1개 게임만 구독 가능      │
│ • 7일 후 자동 만료          │
│ • 광고 시청으로 연장 가능   │
│                             │
│ [광고 보고 시작하기]        │
└─────────────────────────────┘
```

### 3. 만료 임박 알림
```
┌─────────────────────────────┐
│ ⚠️ 프리미엄 만료 D-3        │
│                             │
│ 3일 후 구독이 모두 해제됩니다│
│                             │
│ [프리미엄 연장하기]         │
│ [나중에]                    │
└─────────────────────────────┘
```

---

## 📞 백엔드 API 추가 요청 방법

광고 활성화/결제 활성화 API가 필요하면 아래 정보와 함께 요청해주세요:
1. 사용할 광고 SDK (AdMob, AppLovin 등)
2. 결제 방식 (토스 페이, 애플 인앱결제, 구글 플레이 결제)
3. 가격 정책 (광고권 무료, 프리미엄 월 4,900원 등)

---

## 🎯 체크리스트

앱 개발 시 확인사항:

- [ ] 프리미엄 상태 조회 API 연동
- [ ] 광고 시청 기능 구현
- [ ] 프리미엄 결제 기능 구현
- [ ] 구독 버튼 상태 관리 (무료/광고권/프리미엄 분기)
- [ ] 에러 처리 (403 에러 시 적절한 UI 표시)
- [ ] 만료 알림 UI
- [ ] 프리미엄 가치 전달 UI/UX
